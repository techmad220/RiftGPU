[package]
name = "vrb-backends"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Built-in Vulkan, HIP, CPU, and dynamic plugin backends for Vulkan ROCm Bridge"

[dependencies]
ash.workspace = true
libloading.workspace = true
thiserror.workspace = true
tracing.workspace = true
vrb-core = { version = "0.1.0", path = "../vrb-core" }
vrb-plugin-api = { version = "0.1.0", path = "../vrb-plugin-api" }
